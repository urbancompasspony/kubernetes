>>>
On Rancher Server:
Just kill and remove the container called rancher-kind-control-plane

>>>
On Nodes run:
sudo /usr/local/bin/k3s-uninstall.sh

and remove:

sudo rm -R /var/lib/rancher/
sudo rm -R /etc/rancher/
